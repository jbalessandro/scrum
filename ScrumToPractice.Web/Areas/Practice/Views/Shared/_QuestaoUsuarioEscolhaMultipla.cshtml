@model IEnumerable<ScrumToPractice.Domain.Models.CorResposta>

<hr />
@foreach (ScrumToPractice.Domain.Models.CorResposta item in @Model)
{
    <p>@Html.CheckBoxFor(x => item.SelecaoUsuario, new { id = "Alternativa", @class = "alternativa" }) @item.Resposta.Descricao</p>
}