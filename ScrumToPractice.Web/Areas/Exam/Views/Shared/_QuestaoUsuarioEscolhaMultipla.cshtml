@model IEnumerable<ScrumToPractice.Domain.Models.SimResposta>

<hr />
@foreach (ScrumToPractice.Domain.Models.CorResposta item in @Model)
{
    <p>@Html.CheckBox("alternativa", item.SelecaoUsuario, new { id = "Alternativa", @class = "alternativa" }) @item.Resposta.Descricao</p>
}